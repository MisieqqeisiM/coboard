### To run

#### Requirements:

- docker
- docker-compose

#### Commands:

In development mode:

`./run-dev.sh`

In production mode:

`./run-prod.sh`

The server should then be running on [http://localhost](http://localhost)
